#ifndef COMPILE_MODEL_MEDIAN_H
#define COMPILE_MODEL_MEDIAN_H
#include <vector>
#include <algorithm>
using std::vector;
template <typename T> bool median(const vector<T>&, T&);

#include "median.cpp"
#endif //COMPILE_MODEL_MEDIAN_H
