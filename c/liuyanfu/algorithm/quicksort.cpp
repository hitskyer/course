#include <iostream>

using namespace std;

void qsort(int *arr, size_t left, size_t right)
{
	while (left < right)
	{
		size_t i = left;
		size_t j = right;
		int temp = arr[left];
		for(;i<j && arr[j]>=temp;--j)
		{
			
		}
	}
}
