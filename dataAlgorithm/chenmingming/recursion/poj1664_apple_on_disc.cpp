#include<iostream>
using namespace std;
unsigned long func(size_t apple, size_t disc)
{
    
}
int main()
{
    size_t t, apple, disc;
    cin >> t;
    while(t--)
    {
        cin >> apple >> disc;
        cout << func(apple, disc) << endl;
    }
}